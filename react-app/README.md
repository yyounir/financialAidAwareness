# Financial Aid — React version

This small React app was scaffolded with Vite to host the "Why Financial Aid Still Matters" explainer.

Run locally:

```bash
cd react-app
npm install
npm run dev
```

Open the printed local URL (usually `http://localhost:5173`).

If you prefer `yarn`:

```bash
cd react-app
yarn
yarn dev
```
